# 분석 (Discover)

## 상태

Status: Not Started
Implementation: Not Started
Automation: Not Started
MCP/Plugin: Deferred

## 범위

### 포함

- <점검할 레거시 동작>

### 제외

- <범위 밖 동작>

## 출처

| ID | 출처 유형 | 정제된 참조 | 접근 |
|---|---|---|---|
| SRC-001 | Code / Test / Document | `<공개 가능한 참조>` | Read-only |

## 발견

> **근거 규칙: 인용 없는 근거는 근거가 아니다.** Confirmed는 `파일경로:라인` + 실제 코드 1~3줄 인용이 있을 때만 쓴다.
> 응답값만 보고 판단하지 말 것 — 로직(조건/분기/기본값)을 코드에서 확인한다.

| ID | 출처 | 근거 수준 | 발견 내용 | 인용 (파일:라인 + 코드) |
|---|---|---|---|---|
| EV-001 | SRC-001 | Confirmed / Observed / Inferred | <관찰된 동작> | `XxxService.java:88` "코드 인용" |

## 심문 체크리스트

> **API마다 이 표를 반복한다.** 모든 행은 답변 + `파일:라인` 인용을 갖거나, 상태를 `미확인`으로 두고 OQ-ID를 연다.
> 빈 칸은 "안 물어봤다"는 뜻이다 — 표를 채우려면 실제로 코드를 읽어야 한다.
> ⚡ 표시 항목은 답하는 과정에서 **개선 후보**가 나온다 → `07_Improvements.md`에 기록한다.

### API-001 `<METHOD /path>`

| # | 질문 | 답변 | 근거 (파일:라인) | 상태 |
|---|---|---|---|---|
| 1 | 이 API를 호출하는 화면·이벤트는? (조건부 호출 포함) | | | 확인 / 미확인(OQ-) |
| 2 | 호출 권한은? 권한별로 결과가 달라지는가? | | | |
| 3 | 요청 파라미터의 필수·선택·기본값·검증 규칙은? | | | |
| 4 | 조회 조건(WHERE)에 들어가는 모든 항목과 그 출처는? | | | |
| 5 | 제외되는 레코드가 있는가? (soft delete, 상태값) | | | |
| 6 | 정렬 키와 tie-breaker는? 동점일 때 순서가 보장되는가? | | | |
| 7 | 페이징 기본 size·최대 size는? count를 따로 조회하는가? | | | |
| 8 | 날짜·시간의 형식·timezone·경계(포함/제외)는? | | | |
| 9 | 빈 결과와 null 필드의 응답 형태는? | | | |
| 10 | 예외 발생 시 응답과 로깅은? 조용한 실패가 있는가? | | | |
| 11 | ⚡ 트랜잭션 경계는 어디인가? 외부 호출이 그 안에 있는가? | | | |
| 12 | ⚡ 목록을 순회하면서 추가 쿼리를 호출하는가? | | | |
| 13 | ⚡ 같은 조회가 요청당 여러 번 실행되는가? 결과가 자주 바뀌는가? | | | |
| 14 | ⚡ 조회 조건에 인덱스가 있는가? 전체 스캔 위험은? | | | |
| 15 | 조회인데 쓰기·이력 적재·외부 호출 같은 부수효과가 있는가? | | | |
| 16 | 외부 연동은 직접 호출인가 프록시 경유인가? 실패 시 동작은? | | | |

## 레거시 흐름

> 각 hop마다 실제 파일을 열어 확인하고 인용을 남긴다. 한 hop이라도 건너뛰면 그 지점부터는 Inferred다.

```text
진입점(JSP/URL) → Controller → Service → Client/DAO → 환경설정
   :라인            :라인        :라인       :라인        :라인
```

## 리스크

- <리스크>

## 미결 질문

- <OQ-ID — `99_Open-Questions.md`와 연결>

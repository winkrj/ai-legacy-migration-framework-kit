# 분석 — <기능명>

> 레거시는 read-only. 코드/쿼리/테스트에서 확인한 사실만 적는다. 확인 못 한 것은 추측하지 말고 "미확인"으로 남긴다.
> **인용 없는 근거는 근거가 아니다** — 각 항목에 `파일경로:라인`과 핵심 코드 1~2줄을 붙인다. 응답값만 보지 말고 로직을 확인한다.

## 레거시가 하는 일

- 진입점 (URL/클래스): `파일:라인`
- 흐름 (Controller → ... → 쿼리): 각 hop `파일:라인`
- 조회/필터 조건:
- 정렬/페이징:
- 특이사항 (예외 처리, 숨은 규칙):

## 심문 체크리스트 (축약)

> 빈 칸은 "안 물어봤다"는 뜻이다. 답변에 `파일:라인` 인용을 붙이거나 "미확인"으로 남긴다.
> ⚡ 항목은 답하다 보면 개선 후보가 나온다 → 아래 "개선 후보"에 기록.

| # | 질문 | 답변 + 근거 |
|---|---|---|
| 1 | 이 기능을 호출하는 화면·이벤트는? (조건부 호출 포함) | |
| 2 | 조회 조건·제외 레코드(soft delete, 상태값)는? | |
| 3 | 정렬 tie-breaker와 페이징 기본·최대값은? | |
| 4 | 빈 결과·에러의 응답 형태는? 조용한 실패가 있는가? | |
| 5 | ⚡ 목록 순회 중 추가 쿼리, 반복 조회, 트랜잭션 안 외부 호출이 있는가? | |

## 개선 후보

> **이관은 동작 보존, 개선은 별도 승인.** 기록만 하고 이번 구현에 섞지 않는다.

| 유형 | 발견 내용 | 근거 (파일:라인) | 동작 변경 | 승인 |
|---|---|---|---|---|
| N+1 / 캐싱 / 쿼리 / 트랜잭션 / 구조 | | | Yes / No | Not Approved |

## Target 현재 상태

- (미구현이면 "미구현" 한 줄이면 충분)

## 차이 / 결정이 필요한 것

| # | 내용 | 레거시 동작 | Target/이관 방향 | 누가 결정 |
|---|---|---|---|---|
| 1 |  |  |  |  |

## 미확인 (추측 금지 목록)

- <확인 못 한 동작 — 그대로 구현하면 안 되는 것들>

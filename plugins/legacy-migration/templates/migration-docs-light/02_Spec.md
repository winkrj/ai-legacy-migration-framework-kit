# 스펙 — <기능명>

> 이관 후 동작 계약(SDD). 아래 **구현 승인** 체크 전에는 production 코드를 만지지 않는다.
> **인용 없는 근거는 근거가 아니다** — 레거시 근거는 `파일:라인` + 코드 1~3줄 인용만 인정.

## 공통 규칙

- Approved 컨벤션 참조: <`docs/conventions/` 링크. 재서술 금지, 이 케이스의 예외만 기록>

## 범위 계약

| 항목 | 승인 대상 |
|---|---|
| 이관 범위 | <옮길 화면·API·동작·데이터> |
| 허용 수정 범위 | <생성·수정 가능한 Target 파일·패키지·공유 컴포넌트> |
| 교체·삭제 허용 | <기존 기능·endpoint·파일 중 교체 또는 삭제 가능한 대상> |
| 테스트 범위 | <필수 단위·통합·계약 테스트와 verify 명령> |
| 범위 제외 | <이번에 하지 않는 기능·파일·운영 전환> |

이 표에 포함된 교체·삭제·공유 코드 변경은 승인 후 구현 blocker가 아니다. 표 밖 변경이 필요할 때만 사람에게 범위 확대를 요청한다.

## API 목록

| API ID | Method/Path | 기능명 | 레거시 근거 (파일:라인 + 인용) | 외부 연동 |
|---|---|---|---|---|
| API-001 | `<METHOD /path>` | <기능명> | `Xxx.java:12` "인용" | 없음 / <대상 — 직접·프록시 여부와 환경별 host 확정> |

## API별 상세 스펙

### API-001 <기능명>

#### 목적

<1줄>

#### 시나리오

- **Given** <전제> **When** <행위> **Then** <결과>
- **Given** <빈 결과 전제> **When** <행위> **Then** <빈 결과 동작>

#### Request

- <파라미터 / 필수 여부 / 기본값>

#### Response

- <필드 — 레거시 명칭이 아니라 신규 의미 기준>

#### 오류·빈 결과

- <빈 결과/에러 계약>

#### Acceptance Criteria

- AC-001-1: <검증 가능한 완료 조건>

## 사람이 결정할 것

- [ ] <결정 1 — 예: 삭제된 데이터 노출 여부>
- [ ] <결정 2 — 예: not-found 응답 계약>

## 구현 승인

- [ ] 위 계약대로 구현을 승인한다. 승인 범위를 지속 Goal로 등록하고 구현 → 테스트·검증 → 수정 → 재검증 → 독립 검토를 `READY FOR HUMAN REVIEW`까지 실행한다. (승인자: __________ / 날짜: __________)

# DR-003. Validator CLI Role

Status: Accepted for Local Thin Bootstrap

The Validator CLI remains a separate dependency. It checks Markdown structure and safety candidates, does not mutate source, and does not judge domain correctness.

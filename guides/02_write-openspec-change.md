# Write an OpenSpec Change

Copy `templates/openspec-change/` and choose a lowercase `migrate-<capability>` name.

Write legacy evidence separately from target requirements. Put unresolved behavior in Open Questions and obtain approval before Plan or implementation.

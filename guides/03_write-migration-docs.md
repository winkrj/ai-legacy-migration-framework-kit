# Write Migration Documents

Use `templates/migration-docs/` in workflow order:

`Discover → Specify → OpenSpec → Plan → Implement → Validate → Archive`

Preserve evidence levels, implementation permission and Open Questions throughout the case.
